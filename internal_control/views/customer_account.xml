<odoo>
    <!-- Server Actions -->
    <record id="action_open_accounts_tier_1" model="ir.actions.server">
        <field name="name">Tier 1 Accounts</field>
        <field name="model_id" ref="model_res_partner_account"/>
        <field name="type">ir.actions.server</field>
        <field name="state">code</field>
        <field name="code">action = model.open_accounts_tier_1()</field>
    </record>

    <record id="action_open_accounts_tier_2" model="ir.actions.server">
        <field name="name">Tier 2 Accounts</field>
        <field name="model_id" ref="model_res_partner_account"/>
        <field name="type">ir.actions.server</field>
        <field name="state">code</field>
        <field name="code">action = model.open_accounts_tier_2()</field>
    </record>

    <record id="action_open_accounts_tier_3" model="ir.actions.server">
        <field name="name">Tier 3 Accounts</field>
        <field name="model_id" ref="model_res_partner_account"/>
        <field name="type">ir.actions.server</field>
        <field name="state">code</field>
        <field name="code">action = model.open_accounts_tier_3()</field>
    </record>

    <!-- Tree View Inheritance - Add fields and disable create/delete -->
    <record id="internal_control_customer_account_tree_inherit" model="ir.ui.view">
        <field name="name">res.customer.account.tree.inherit</field>
        <field name="model">res.partner.account</field>
        <field name="inherit_id" ref="compliance_management.compliance_customer_account_tree"/>
        <field name="arch" type="xml">
            <!-- Disable create and delete in tree view -->
            <tree position="attributes">
                <attribute name="create">false</attribute>
                <attribute name="delete">false</attribute>
            </tree>

            <!-- Replace account_name field -->
            <field name="account_name" position="replace">
                <field name="accounttitle" string="Account Title" optional="show"/>
                <field name="account_tier" string="Account Tier" optional="show"/>
            </field>

            <!-- Remove fields -->
            <field name="risk_level" position="replace"/>
            <field name="tier_name" position="replace"/>

            <!-- Add additional fields -->
            <tree position="inside">
                <field name="lnbalance" string="Ln Balance" optional="hide"/>
                <field name="bkbalance" string="Bk Balance" optional="hide"/>
                <field name="unclearedbal" string="Uncleared Balance" optional="hide"/>
                <field name="holdbal" string="Hold Balance" optional="hide"/>
                <field name="totdebit" string="Total Debit" optional="hide"/>
                <field name="totcredit" string="Total Credit" optional="hide"/>
                <field name="last_month_balance" string="Last Month Balance" optional="hide"/>
                <field name="lien" string="Lien" optional="hide"/>
                <field name="source_account_id" string="Source Account ID" optional="hide"/>
                <field name="status" widget="boolean" string="Status" optional="hide"/>
            </tree>
        </field>
    </record>

    <!-- Form View Inheritance - Disable create/delete -->
    <record id="customer_account_view_form_inherit" model="ir.ui.view">
        <field name="name">customer.account.view.form.inherit</field>
        <field name="model">res.partner.account</field>
        <field name="inherit_id" ref="compliance_management.customer_account_view_form"/>
        <field name="arch" type="xml">
            <!-- Disable create and delete in form view -->
            <form position="attributes">
                <attribute name="create">false</attribute>
                <attribute name="delete">false</attribute>
            </form>

            <!-- Replace account_name label -->
            <xpath expr="//label[@for='account_name']" position="replace">
                <label for="accounttitle" string="Account Title"/>
            </xpath>

            <!-- Replace account_name field -->
            <xpath expr="//field[@name='account_name']" position="replace">
                <field name="accounttitle" class="oe_inline" readonly="1"/>
            </xpath>

            <field name="tier_name" position="replace"/>

            <!-- Remove unnecessary fields -->
            <xpath expr="//field[@name='customer_id']" position="replace"/>
            <xpath expr="//field[@name='risk_assessment']" position="replace"/>
            <xpath expr="//sheet//button[@name='get_risk_level']" position="replace"/>
            <xpath expr="//sheet//button[@name='get_risk_score']" position="replace"/>

            <!-- Add new field groups -->
            <xpath expr="//sheet" position="inside">
                <group string="Additional Details">
                    <group>
                        <field name="accounttitle" string="Account Title" readonly="1"/>
                        <field name="officercode" string="Account Officer" readonly="1"/>
                        <field name="sectorcode" string="Sector" readonly="1"/>
                        <field name="account_tier" string="Account Tier" readonly="1"/>
                        <field name="source_account_id" string="Source Account ID" readonly="1"/>
                    </group>
                    <group>
                        <field name="lnbalance" string="Ln Balance" readonly="1"/>
                        <field name="bkbalance" string="Bk Balance" readonly="1"/>
                        <field name="unclearedbal" string="Uncleared Balance" readonly="1"/>
                        <field name="holdbal" string="Hold Balance" readonly="1"/>
                        <field name="status" widget="boolean" string="Status" readonly="1"/>
                    </group>
                </group>
                <group string="Balance Summary">
                    <group>
                        <field name="totdebit" string="Total Debit" readonly="1"/>
                        <field name="totcredit" string="Total Credit" readonly="1"/>
                    </group>
                    <group>
                        <field name="last_month_balance" string="Last Month Balance" readonly="1"/>
                        <field name="lien" string="Lien" readonly="1"/>
                        <field name="bank_product_id" string="Bank Product" readonly="1"/>
                    </group>
                </group>
            </xpath>
        </field>
    </record>

</odoo>
