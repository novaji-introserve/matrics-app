<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Readonly Tree View -->
    <record id="case_list_closed_tree_view" model="ir.ui.view">
        <field name="name">case.list.closed</field>
        <field name="model">case</field>
        <field name="arch" type="xml">
            <tree string="Closed Cases" create="false" edit="false" delete="false">
                <field name="name"/>
                <field name="status_id"/>
                <field name="created_at"/>
                <field name="staff_id"/>
                <field name="team_id"/>
            </tree>
        </field>
    </record>

    <!-- Readonly Form View (define this before referencing it) -->
    <record id="case_closed_form_view" model="ir.ui.view">
        <field name="name">case.closed.form</field>
        <field name="model">case</field>
        <field name="arch" type="xml">
            <form string="Closed Case" create="false" edit="false" delete="false">
                <sheet>
                    <group>
                        <field name="name" readonly="1"/>
                        <field name="status_id" readonly="1"/>
                        <field name="created_at" string="Start Date" readonly="1"/>
                        <field name="end_date" string="End Date" readonly="1"/>
                        <field name="staff_id" string="Staff Responsibile" readonly="1"/>
                        <field name="team_id" readonly="1"/>
                        <field name="staff_user_id" invisible="1"/>
                        <field name="response_note" string="Report Response" readonly="1"/>
                        <field name="cases_action" readonly="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action that references both views -->
    <record id="closed_case_action" model="ir.actions.act_window">
        <field name="name">All Closed Cases</field>
        <field name="res_model">case</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="case_list_closed_tree_view"/>
        <field name="domain">[('status_id.name', '=', 'closed')]</field>
        <field name="context">{'create': False}</field>
    </record>

    <!-- <record id="closed_case_action" model="ir.actions.act_window">
        <field name="name">All Closed Cases</field>
        <field name="res_model">case</field>
        <field name="view_mode">tree,form</field>
        <field name="view_ids" eval="[
            (0, 0, {'view_mode': 'tree', 'view_id': ref('case_list_closed_tree_view')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('case_closed_form_view')})
        ]"/>
        <field name="domain">[('status_id.name', '=', 'closed')]</field>
        <field name="context">{'create': False}</field>
    </record> -->

</odoo>
