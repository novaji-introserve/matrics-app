<?xml version="1.0" encoding="UTF-8" ?>
<templates>
    <t t-name="owl.KpiCard" owl="1">
        <div class="case-kpi-card" t-on-click="() => props.onClick(props.stat)">
            <div class="case-kpi-card__value"><span t-esc="props.value"/></div>
            <div class="case-kpi-card__label"><span t-esc="props.name"/></div>
            <div t-att-class="deltaClass">
                <span t-esc="percentageLabel"/>
            </div>
        </div>
    </t>
</templates>

<!-- <?xml version="1.0" encoding="UTF-8" ?>
<templates>
    <t t-name="owl.KpiCard" owl="1">
        <div class="col-6 col-md-4 col-lg-2.4" style="flex: 0 0 20%; max-width: 20%;">
            <div class="shadow-sm border p-3 bg-white text-center cursor-pointer h-100" t-on-click="()=>props.onClick()">
                <div class="h1 fw-bold text-dark mb-2">
                    <span t-esc="props.value"/>
                </div>
                <div class="h6 text-muted m-0">
                    <span t-esc="props.name"/>
                </div>
            </div>
        </div>
    </t>
</templates> -->
