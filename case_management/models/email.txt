LATEST

def _send_case_closure_alert(self):
        """Send email alert when a case is closed"""
        self.ensure_one()
        template = self.env.ref('case_management.case_closure_alert_template')

        if not template:
            _logger.error("Case closure email template not found")
            return False
        
        # Prepare recipient data
        staff_user = self.staff_id
        creator_user = self.user_id
        supervisors = [self.supervisor_one_id]
        if self.supervisor_two_id:
            supervisors.append(self.supervisor_two_id)
        if self.supervisor_three_id:
            supervisors.append(self.supervisor_three_id)
        
        # CC list - creator and supervisors
        cc_emails = []
        if creator_user and creator_user.email:
            cc_emails.append(creator_user.email)
        for supervisor in supervisors:
            if supervisor and supervisor.email:
                cc_emails.append(supervisor.email)
        
        # Prepare mail values
        mail_values = {
            'email_to': staff_user.email if staff_user else '',
            'email_cc': ','.join(cc_emails),
            'email_from': 'noreply@example.com',
        }

        for rec in self:
            # Extract values directly from the record fields (no self in ctx)
            event_date = rec.event_date
            alert_name = rec.name
            title = rec.cases_description
            rating_name = rec.rating_id.name if rec.rating_id else ''
            staff_dept = rec.team_id.name if rec.team_id else ''
            status_name = rec.status_name
            exception_process = rec.process_id.name if rec.process_id else ''
            process_type = rec.root_category_id.name if rec.root_category_id else ''
            process_category = rec.process_category_id.name if rec.process_category_id else ''
            case_action = rec.cases_action
            close_remarks = "Case closed successfully"
            user_name = rec.user_id.name
            user_email = rec.user_id.email
            response_link = f'/web#id={rec.id}&model=case&view_type=form'  # Added this line
            
            # Context for email template
            close_case = {
                'event_date': event_date,
                'alert_name': alert_name,
                'title': title,
                'rating_name': rating_name,
                'staff_dept': staff_dept,
                'status_name': status_name,
                'exception_process': exception_process,
                'process_type': process_type,
                'process_category': process_category,
                'case_action': case_action,
                'close_remarks': close_remarks,
                'user_name': user_name,
                'user_email': user_email,
                'response_link': response_link,  # Added this line
                'creator_user_name': creator_user.name if creator_user else '',  # Added creator info for consistency
                'creator_user_email': creator_user.email if creator_user else ''
            }

            _logger.info("Rendering template with context:")
            for k, v in close_case.items():
                _logger.info(f"{k}: {v}")

            try:
                # Use the template with the context
                template_id = template.with_context(**close_case)
                
                # Render the email content using the context
                rendered_html = template_id._render_template(
                    template_id.body_html,
                    template_id.model,
                    [rec.id],  # Changed from self.id to rec.id
                    engine='qweb',
                    add_context=close_case
                )[rec.id]  # Changed from self.id to rec.id

                # Optional: log the rendered HTML
                _logger.debug(f"Rendered email HTML for case {rec.id}:\n{rendered_html}")

                # Send the email
                email_result = template_id.send_mail(
                    rec.id,  # Changed from self.id to rec.id
                    force_send=True,
                    raise_exception=True,
                    email_values=mail_values
                )

                _logger.info(f"Case closure alert sent for case {rec.id}")
                return True

            except Exception as e:
                _logger.error(f"Failed to send case closure alert for case {rec.id}: {str(e)}")
                return False
























def _send_case_closure_alert(self):
        """Send email alert when a case is closed"""
        self.ensure_one()
        template = self.env.ref('case_management.case_closure_alert_template')

        if not template:
            _logger.error("Case closure email template not found")
            return False
        
        # Prepare recipient data
        staff_user = self.staff_id
        creator_user = self.user_id
        supervisors = [self.supervisor_one_id]
        if self.supervisor_two_id:
            supervisors.append(self.supervisor_two_id)
        if self.supervisor_three_id:
            supervisors.append(self.supervisor_three_id)
        
        # CC list - creator and supervisors
        cc_emails = []
        if creator_user and creator_user.email:
            cc_emails.append(creator_user.email)
        for supervisor in supervisors:
            if supervisor and supervisor.email:
                cc_emails.append(supervisor.email)
        
        # Prepare mail values
        mail_values = {
            'email_to': staff_user.email if staff_user else '',
            'email_cc': ','.join(cc_emails),
            'email_from': 'noreply@example.com',
        }

        for rec in self:
            # Extract values directly from the record fields (no self in ctx)
            event_date = rec.event_date
            alert_name = rec.name
            title = rec.cases_description
            rating_name = rec.rating_id.name if rec.rating_id else ''
            staff_dept = rec.team_id.name if rec.team_id else ''
            status_name = rec.status_name
            exception_process = rec.process_id.name if rec.process_id else ''
            process_type = rec.root_category_id.name if rec.root_category_id else ''
            process_category = rec.process_category_id.name if rec.process_category_id else ''
            case_action = rec.cases_action
            close_remarks = "Case closed successfully"
            user_name = rec.user_id.name
            user_email = rec.user_id.email
        
        # Context for email template
        close_case = {
            'event_date': event_date,
            'alert_name': alert_name,
            'title': title,
            'rating_name': rating_name,
            'staff_dept': staff_dept,
            'status_name': status_name,
            'exception_process': exception_process,
            'process_type': process_type,
            'process_category': process_category,
            'case_action': case_action,
            'close_remarks': close_remarks,
            'user_name': user_name,
            'user_email': user_email
        }

        _logger.info("Rendering template with context:")
        for k, v in close_case.items():
            _logger.info(f"{k}: {v}")

        try:
            # Use the template with the context
            template_id = template.with_context(**close_case)
            
            # # Render the email content using the context
            # rendered_html = template_id._render_template(
            #     template_id.body_html,
            #     template_id.model,
            #     [self.id],
            #     engine='qweb',
            #     add_context=close_case
            # )[self.id]

            # Optional: log the rendered HTML
            # _logger.debug(f"Rendered email HTML for case {rec.id}:\n{rendered_html}")
            _logger.debug(f"Rendered email HTML for case {rec.id}:\n{template_id}")

            # Send the email
            email_result = template_id.send_mail(
                self.id,
                force_send=True,
                raise_exception=True,
                email_values=mail_values
            )

            _logger.info(f"Case closure alert sent for case {self.id}")
            return True

        except Exception as e:
            _logger.error(f"Failed to send case closure alert for case {self.id}: {str(e)}")
            return False














def _send_case_creation_alert(self):
        """Send email alert when a case is created"""
        self.ensure_one()
        template = self.env.ref('case_management.case_creation_alert_template')

        if not template:
            _logger.error("Case creation email template not found")
            return False
            
        # Prepare recipient data
        staff_user = self.staff_id
        creator_user = self.user_id
        supervisors = [self.supervisor_one_id]
        if self.supervisor_two_id:
            supervisors.append(self.supervisor_two_id)
        if self.supervisor_three_id:
            supervisors.append(self.supervisor_three_id)
            
        # CC list - creator and supervisors
        cc_emails = []
        if creator_user and creator_user.email:
            cc_emails.append(creator_user.email)
        for supervisor in supervisors:
            if supervisor and supervisor.email:
                cc_emails.append(supervisor.email)
                
        # Prepare mail values
        mail_values = {
            'email_to': staff_user.email if staff_user else '',
            'email_cc': ','.join(cc_emails),
            'email_from': 'noreply@example.com',
        }
        
        # Add data for template
        ctx = {
            'event_date': self.event_date,
            'alert_name': self.name,
            'title': self.cases_description,
            'rating_name': self.rating_id.name if self.rating_id else '',
            'staff_dept': self.team_id.name if self.team_id else '',
            'status_name': self.status_name,
            'exception_process': self.process_id.name if self.process_id else '',
            'process_type': self.root_category_id.name if self.root_category_id else '',
            'process_category': self.process_category_id.name if self.process_category_id else '',
            'case_action': self.cases_action,
            'description': self.further_description,
            'response_link': f'/web#id={self.id}&model=case&view_type=form'
        }
        
        try:
            # Send email using the CASE ID rather than partner ID
            template.with_context(**ctx).send_mail(
                self.id,  # Use the case ID instead of partner ID
                force_send=True,
                email_values=mail_values
            )
            _logger.info(f"Case creation alert sent for case {self.id}")
            return True
        except Exception as e:
            _logger.error(f"Failed to send case creation alert for case {self.id}: {str(e)}")
            return False
            
    def _send_case_response_alert(self, response):
        """Send email alert when a case receives a response"""
        self.ensure_one()
        template = self.env.ref('case_management.case_response_alert_template')

        if not template:
            _logger.error("Case response email template not found")
            return False
            
        # Prepare recipient data
        creator_user = self.user_id
        staff_user = self.staff_id
        responder_user = response.create_uid
        supervisors = [self.supervisor_one_id]
        if self.supervisor_two_id:
            supervisors.append(self.supervisor_two_id)
        if self.supervisor_three_id:
            supervisors.append(self.supervisor_three_id)
            
        # CC list - staff and supervisors
        cc_emails = []
        if staff_user and staff_user.email:
            cc_emails.append(staff_user.email)
        for supervisor in supervisors:
            if supervisor and supervisor.email:
                cc_emails.append(supervisor.email)
                
        # Prepare mail values
        mail_values = {
            'email_to': creator_user.email if creator_user else '',
            'email_cc': ','.join(cc_emails),
            'email_from': 'noreply@example.com',
        }
        
        # Add data for template
        ctx = {
            'event_date': self.event_date,
            'alert_name': self.name,
            'title': self.cases_description,
            'rating_name': self.rating_id.name if self.rating_id else '',
            'staff_dept': self.team_id.name if self.team_id else '',
            'status_name': self.status_name,
            'exception_process': self.process_id.name if self.process_id else '',
            'process_type': self.root_category_id.name if self.root_category_id else '',
            'process_category': self.process_category_id.name if self.process_category_id else '',
            'case_action': self.cases_action,
            'response': response.response,
            'user_name': self.user_id.name,
            'user_email': self.user_id.email,
            'responder_name': responder_user.name,
            'responder_email': responder_user.email
        }
        
        try:
            # Send email using the CASE ID rather than partner ID
            template.with_context(**ctx).send_mail(
                self.id,  # Use the case ID instead of partner ID
                force_send=True,
                email_values=mail_values
            )
            _logger.info(f"Case response alert sent for case {self.id}")
            return True
        except Exception as e:
            _logger.error(f"Failed to send case response alert for case {self.id}: {str(e)}")
            return False

    def _send_case_closure_alert(self):
        """Send email alert when a case is closed"""
        self.ensure_one()
        template = self.env.ref('case_management.case_closure_alert_template')

        if not template:
            _logger.error("Case closure email template not found")
            return False
            
        # Prepare recipient data
        staff_user = self.staff_id
        creator_user = self.user_id
        supervisors = [self.supervisor_one_id]
        if self.supervisor_two_id:
            supervisors.append(self.supervisor_two_id)
        if self.supervisor_three_id:
            supervisors.append(self.supervisor_three_id)
            
        # CC list - creator and supervisors
        cc_emails = []
        if creator_user and creator_user.email:
            cc_emails.append(creator_user.email)
        for supervisor in supervisors:
            if supervisor and supervisor.email:
                cc_emails.append(supervisor.email)
                
        # Prepare mail values
        mail_values = {
            'email_to': staff_user.email if staff_user else '',
            'email_cc': ','.join(cc_emails),
            'email_from': 'noreply@example.com',
        }
        
        # Add data for template - this passes close_case as a dictionary
        # because the template uses close_case[] rather than object
        close_case = {
            'event_date': self.event_date,
            'alert_name': self.name,
            'title': self.cases_description,
            'rating_name': self.rating_id.name if self.rating_id else '',
            'staff_dept': self.team_id.name if self.team_id else '',
            'status_name': self.status_name,
            'exception_process': self.process_id.name if self.process_id else '',
            'process_type': self.root_category_id.name if self.root_category_id else '',
            'process_category': self.process_category_id.name if self.process_category_id else '',
            'case_action': self.cases_action,
            'close_remarks': "Case closed successfully",
            'user_name': self.user_id.name,
            'user_email': self.user_id.email
        }
        
        try:
            # Send email using the CASE ID rather than partner ID
            template.with_context(**close_case).send_mail(
                self.id,  # Use the case ID instead of partner ID
                force_send=True,
                email_values=mail_values
            )
            _logger.info(f"Case closure alert sent for case {self.id}")
            return True
        except Exception as e:
            _logger.error(f"Failed to send case closure alert for case {self.id}: {str(e)}")
            return False























def _send_case_creation_alert(self):
        """Send email alert when a case is created"""
        self.ensure_one()
        #template = self.env.ref('compliance_management.case_creation_alert_template')
        template = self.env.ref('case_management.case_creation_alert_template')

        if not template:
            _logger.error("Case creation email template not found")
            return False
            
        # Prepare recipient data
        staff_user = self.staff_id
        creator_user = self.user_id
        supervisors = [self.supervisor_one_id]
        if self.supervisor_two_id:
            supervisors.append(self.supervisor_two_id)
        if self.supervisor_three_id:
            supervisors.append(self.supervisor_three_id)
            
        # CC list - creator and supervisors
        cc_emails = []
        if creator_user and creator_user.email:
            cc_emails.append(creator_user.email)
        for supervisor in supervisors:
            if supervisor and supervisor.email:
                cc_emails.append(supervisor.email)
                
        # Prepare mail values
        mail_values = {
            'email_to': staff_user.email if staff_user else '',
            'email_cc': ','.join(cc_emails),
            'email_from': 'noreply@example.com',
        }
        
        # Add data for template
        ctx = {
            'event_date': self.event_date,
            'alert_name': self.name,
            'title': self.cases_description,
            'rating_name': self.rating_id.name if self.rating_id else '',
            'staff_dept': self.team_id.name if self.team_id else '',
            'status_name': self.status_name,
            'exception_process': self.process_id.name if self.process_id else '',
            'process_type': self.root_category_id.name if self.root_category_id else '',
            'process_category': self.process_category_id.name if self.process_category_id else '',
            'case_action': self.cases_action,
            'description': self.further_description,
            'response_link': f'/web#id={self.id}&model=case&view_type=form'
        }
        
        
        try:
            # Check for valid partner ID first
            partner_id = self.staff_id.partner_id.id if self.staff_id and self.staff_id.partner_id else False
            if not partner_id:
                _logger.warning(f"No valid partner found for staff on case {self.id}")
                return False
                
            # Send email with context data
            template.with_context(ctx).send_mail(partner_id, force_send=True, email_values=mail_values)
            _logger.info(f"Case creation alert sent for case {self.id}")
            return True
        except Exception as e:
            _logger.error(f"Failed to send case creation alert: {str(e)}")
            return False
        
        # try:
        #     # Send email with context data
        #     template.with_context(ctx).send_mail(
        #         self.staff_id.partner_id.id if self.staff_id and self.staff_id.partner_id else self.env.user.partner_id.id,
        #         force_send=True,
        #         email_values=mail_values
        #     )
        #     _logger.info(f"Case creation alert sent for case {self.id}")
        #     return True
        # except Exception as e:
        #     _logger.error(f"Failed to send case creation alert: {str(e)}")
        #     return False
            
    def _send_case_response_alert(self, response):
        """Send email alert when a case receives a response"""
        self.ensure_one()
       # template = self.env.ref('compliance_management.case_response_alert_template')
        template = self.env.ref('case_management.case_response_alert_template')

        if not template:
            _logger.error("Case response email template not found")
            return False
            
        # Prepare recipient data
        creator_user = self.user_id
        staff_user = self.staff_id
        responder_user = response.create_uid
        supervisors = [self.supervisor_one_id]
        if self.supervisor_two_id:
            supervisors.append(self.supervisor_two_id)
        if self.supervisor_three_id:
            supervisors.append(self.supervisor_three_id)
            
        # CC list - staff and supervisors
        cc_emails = []
        if staff_user and staff_user.email:
            cc_emails.append(staff_user.email)
        for supervisor in supervisors:
            if supervisor and supervisor.email:
                cc_emails.append(supervisor.email)
                
        # Prepare mail values
        mail_values = {
            'email_to': creator_user.email if creator_user else '',
            'email_cc': ','.join(cc_emails),
            'email_from': 'noreply@example.com',
        }
        
        # Add data for template
        ctx = {
            'event_date': self.event_date,
            'alert_name': self.name,
            'title': self.cases_description,
            'rating_name': self.rating_id.name if self.rating_id else '',
            'staff_dept': self.team_id.name if self.team_id else '',
            'status_name': self.status_name,
            'exception_process': self.process_id.name if self.process_id else '',
            'process_type': self.root_category_id.name if self.root_category_id else '',
            'process_category': self.process_category_id.name if self.process_category_id else '',
            'case_action': self.cases_action,
            'response': response.response,
            'user_name': self.user_id.name,
            'user_email': self.user_id.email,
            'responder_name': responder_user.name,
            'responder_email': responder_user.email
        }
        
        
        try:
            # Check for valid partner ID first
            partner_id = self.user_id.partner_id.id if self.user_id and self.user_id.partner_id else False
            if not partner_id:
                _logger.warning(f"No valid partner found for creator on case {self.id}")
                return False
                
            # Send email with context data
            template.with_context(ctx).send_mail(partner_id, force_send=True, email_values=mail_values)
            _logger.info(f"Case response alert sent for case {self.id}")
            return True
        except Exception as e:
            _logger.error(f"Failed to send case response alert: {str(e)}")
            return False
                
        # try:
        #     # Send email with context data
        #     template.with_context(ctx).send_mail(
        #         self.user_id.partner_id.id if self.user_id and self.user_id.partner_id else self.env.user.partner_id.id,
        #         force_send=True,
        #         email_values=mail_values
        #     )
        #     _logger.info(f"Case response alert sent for case {self.id}")
        #     return True
        # except Exception as e:
        #     _logger.error(f"Failed to send case response alert: {str(e)}")
        #     return False
            
    def _send_case_closure_alert(self):
        """Send email alert when a case is closed"""
        self.ensure_one()
       # template = self.env.ref('compliance_management.case_closure_alert_template')
        template = self.env.ref('case_management.case_closure_alert_template')

        if not template:
            _logger.error("Case closure email template not found")
            return False
            
        # Prepare recipient data
        staff_user = self.staff_id
        creator_user = self.user_id
        supervisors = [self.supervisor_one_id]
        if self.supervisor_two_id:
            supervisors.append(self.supervisor_two_id)
        if self.supervisor_three_id:
            supervisors.append(self.supervisor_three_id)
            
        # CC list - creator and supervisors
        cc_emails = []
        if creator_user and creator_user.email:
            cc_emails.append(creator_user.email)
        for supervisor in supervisors:
            if supervisor and supervisor.email:
                cc_emails.append(supervisor.email)
                
        # Prepare mail values
        mail_values = {
            'email_to': staff_user.email if staff_user else '',
            'email_cc': ','.join(cc_emails),
            'email_from': 'noreply@example.com',
        }
        
        # Add data for template - this passes close_case as a dictionary
        # because the template uses close_case[] rather than object
        close_case = {
            'event_date': self.event_date,
            'alert_name': self.name,
            'title': self.cases_description,
            'rating_name': self.rating_id.name if self.rating_id else '',
            'staff_dept': self.team_id.name if self.team_id else '',
            'status_name': self.status_name,
            'exception_process': self.process_id.name if self.process_id else '',
            'process_type': self.root_category_id.name if self.root_category_id else '',
            'process_category': self.process_category_id.name if self.process_category_id else '',
            'case_action': self.cases_action,
            'close_remarks': "Case closed successfully",
            'user_name': self.user_id.name,
            'user_email': self.user_id.email
        }
        
        
        try:
            # Check for valid partner ID first
            partner_id = self.staff_id.partner_id.id if self.staff_id and self.staff_id.partner_id else False
            if not partner_id:
                _logger.warning(f"No valid partner found for staff on case {self.id}")
                return False
                
            # Send email with context data
            template.with_context(close_case=close_case).send_mail(partner_id, force_send=True, email_values=mail_values)
            _logger.info(f"Case closure alert sent for case {self.id}")
            return True
        except Exception as e:
            _logger.error(f"Failed to send case closure alert: {str(e)}")
            return False
        
        # try:
        #     # Send email with context data
        #     template.with_context(close_case=close_case).send_mail(
        #         self.staff_id.partner_id.id if self.staff_id and self.staff_id.partner_id else self.env.user.partner_id.id,
        #         force_send=True,
        #         email_values=mail_values
        #     )
        #     _logger.info(f"Case closure alert sent for case {self.id}")
        #     return True
        # except Exception as e:
        #     _logger.error(f"Failed to send case closure alert: {str(e)}")
        #     return False
    