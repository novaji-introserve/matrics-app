<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">
        <!-- Odoo Cron DISABLED - Using standalone alert_scheduler.py instead -->
        <!-- See docker-compose.yml: alert_scheduler service -->
        <!-- For dev: Run manually with: python3 /path/to/alert_scheduler.py -->
        
        <!-- Kept for reference only - NOT ACTIVE -->
        <!--
        <record id="ir_cron_alert_rules_processing" model="ir.cron">
            <field name="name">Process Alert Rules (DISABLED - Use standalone scheduler)</field>
            <field name="model_id" ref="alert_management.model_alert_rules" />
            <field name="state">code</field>
            <field name="code">model.process_alert_rules()</field>
            <field name="active" eval="False" />
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
        </record>
        -->
    </data>
</odoo>
