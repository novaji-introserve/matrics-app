<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- <record id="compliance_alert_history_tree" model="ir.ui.view">
        <field name="name">compliance.alert.history.tree</field>
        <field name="model">alert_history</field>
        <field name="inherit_id" ref="alert_management.view_alert_history_tree"/>
        <field name="arch" type="xml">
            <field name="your_new_field_1" position="after">
                <field name="your_new_field_2"/>
            </field>
            </field>
    </record>

    <record id="compliance_alert_history_form" model="ir.ui.view">
        <field name="name">compliance.alert.history.form</field>
        <field name="model">alert.history</field>
        <field name="inherit_id" ref="alert_management.view_alert_history_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@string='RISK RATING']" position="after">
                <group string="COMPLIANCE INFORMATION">
                    <field name="compliance_status"/>
                    </group>
            </xpath>
            <xpath expr="//header/button[@name='generate_csv']" position="after">
                <button name="your_new_button" type="object" string="New Action" class="oe_highlight"/>
            </xpath>
            <xpath expr="//notebook/page[@string='Record']" position="after">
                <page string="Compliance Details">
                    <field name="compliance_notes" widget="html"/>
                </page>
            </xpath>
        </field>
    </record>

    <record id="compliance_action_alert_history_status" model="ir.actions.act_window">
        <field name="name">Compliance Alert History</field>
        <field name="res_model">alert.history</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('user_in_emails', '=', True), ('compliance_status', '=', 'pending')]</field>
        <field name="context">{'default_compliance_status': 'pending'}</field>
        <field name="inherit_id" ref="alert_management.action_alert_history_status"/> -->
    </record>
</odoo>