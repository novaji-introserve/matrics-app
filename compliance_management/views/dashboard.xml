<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- <record id="compliance_dashboard_view_kanban" model="ir.ui.view">
        <field name="name">rec.compliance.stat.view.kanban</field>
        <field name="model">res.compliance.stat</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" default_group_by="scope" create="false">
                <field name="name" />
                <field name="val" />
                <field name="write_date" />
                <field name="scope_color" />
                <field name="scope" />
                <templates>
                    <t t-name="kanban-box"> -->

                     

                        <!-- <div t-att-class="oe_kanban_global_click" t-attf-style="justify-content: space-between; min-height: 20vh; margin-bottom: 0.4rem;"> -->
                        <!-- <div t-att-class="oe_kanban_global_click"
                            t-att-style="justify-content: space-between; min-height: 20vh; margin-bottom: 0.4rem;"> -->
                            
                        
                            <!--  <div class="o_kanban_image">
                                <img alt="Stat Image" src="compliance_management/static/description/icons-compliance.png" class="o_kanban_image"/>
                            </div>-->
                            <!-- <div class="">
                                <p class="text-muted display-6">
                                    <field name="name" />
                                    
                                </p>
                                <div>
                                    <h1 style="line-height: 1rem; font-size: 3rem;">
                                        <field name="val" decoration-info="1" />
                                    </h1>
                                </div> -->
                                <!--<div t-if="record.function.raw_value"><field name="function"/></div>
                                <div t-if="record.email.raw_value"><field name="email" widget="email"/></div>
                                <div t-if="record.type.raw_value != 'contact'">
                                    <div>
                                        <field name="zip"/><t t-if="record.city"> </t>
                                        <field name="city"/>
                                    </div>
                                    <field t-if="record.state_id.raw_value" name="state_id"/><t t-if="record.country_id"> </t>
                                    <field name="country_id"/>
                                </div>
                                <div t-if="record.phone.raw_value">Phone: <t t-esc="record.phone.value"/></div>
                                <div t-if="record.mobile.raw_value">Mobile: <t t-esc="record.mobile.value"/></div>-->
                            <!-- </div> -->
                            <!--  <field name="name" class="fw-bolder fs-5" /><br/>
                            <field name="val" widget="badge" decoration-info="1"/> -->
                        <!-- </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="action_dashboard_compliance" model="ir.actions.act_window">
        <field name="name">Overview</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.compliance.stat</field>
        <field name="view_id" ref="compliance_dashboard_view_kanban" />
        <field name="view_mode">kanban,tree</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Compliance Statistics
            </p>
            <p>
                No records yet
            </p>
        </field>
    </record>
 -->

    <!-- client to load the owl action created -->
    <record id="action_compliance_dashboard" model="ir.actions.client">
        <field name="name">Dashboard</field>
        <field name="tag">owl.compliance_dashboard</field>
    </record>

    <menuitem id="menu_compliance_dashboard" name="Dashboard" action="action_compliance_dashboard"
        parent="compliance_management.menu_compliance_root" sequence="-1" />

</odoo>